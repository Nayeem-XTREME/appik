import styled from "styled-components"
import {
  space,
  layout,
  position,
  color,
  typography,
  border,
} from "styled-system"

const Box = styled.div`
  ${space};
  ${layout};
  ${position};
  ${color};
  ${typography};
  ${border};
`

export default Box