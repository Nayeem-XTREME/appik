import styled from "styled-components"
import {
  space,
  layout,
  position,
  color,
  typography,
  border,
  background
} from "styled-system"

const Box = styled.div`
  ${space};
  ${layout};
  ${position};
  ${color};
  ${typography};
  ${border};
  ${background}
`

export default Box